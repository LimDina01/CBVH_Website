services:
  app:
    build:
      context: .
    ports:
      - "8000:80"
    volumes:
      - database_volume:/var/www/html/database
    environment:
      - DB_CONNECTION=sqlite
      - DB_DATABASE=/var/www/html/database/database.sqlite

volumes:
  database_volume:
